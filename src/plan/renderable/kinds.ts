export const RENDER_STRATEGY = {
    REFERENCE: 'reference',
    INLINE: 'inline',
} as const

export const RENDERABLE_UNION_SHAPE = {
    COLLAPSED: 'collapsed',
    VARIANTS: 'variants',
} as const
