fragment Base on Query {
    user {
        id
    }
}

fragment Extra on Query {
    user {
        name
    }
}

query Composed($details: Boolean!) {
    ...Base
    ...Extra @include(if: $details)
}
